from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
SOURCE = ROOT / "results_paper_materials_v3" / "full_paper_body_v5_unsw.md"
OUT = ROOT / "results_paper_materials_v3" / "full_paper_body_v6_data_processing.md"

SECTION = '''### 3.2 数据来源、清洗和审计

CIC-IDS2017数据来自已下载并解压的`MachineLearningCSV`，原始目录包含8个CSV文件、2,830,743条记录和78个流量统计特征。本文逐阶段记录数据处理结果。首先规范化列名，以`Label`列为标签源，按预先锁定规则映射为Normal、DoS/DDoS、Brute Force、Web Attack和Bot五类；PortScan、Infiltration和Heartbleed在主任务中排除。映射阶段保留2,671,766条记录，排除标签记录158,977条。

清洗阶段对78个特征强制执行数值转换，并将正负无穷统一视为无效值；任一特征出现非数值、NaN或无穷的记录均删除。原始数据中共发现2,741条无效记录，均属于无穷或非有限值，普通缺失值记录为0条，最终得到2,669,025条有效数值记录。

为降低重复流量向量造成的评估偏差，本文在划分前进行全局去重。每条数值特征向量按原始列顺序和逆序列顺序计算两个64位哈希，组合成128位复合指纹；哈希只用于候选重复定位，保留记录仍使用完整向量核查。审计发现239,101条重复记录，其中同标签重复234,577条，跨标签不一致4,524条。跨标签冲突涉及133个唯一特征向量和5,048条原始记录；剔除这133个冲突向量后，唯一向量数由2,429,924降为2,429,791。

去重和冲突剔除后，按目标类别设置每类20,000条上限，得到53,237条候选记录；再按五个主类别中的最小可用规模等量抽样，形成3,365条平衡研究子集，每类673条。采用固定随机种子42进行分层70%/15%/15%划分，训练集、验证集和测试集分别为2,355、505和505条，每个split中五类数量分别为471、101和101。抽样、划分和模型随机状态均写入配置文件。

每条处理后样本的原始文件名、原始路径、文件内行号、原始标签和映射后标签均保存于`*_source_provenance.csv`旁车文件中，不进入模型特征。分块读取时，文件内行号使用全局递增偏移，确保同一原始文件的来源行号唯一。三份来源侧写覆盖对应split的全部样本，来源元数据与模型输入完全分离。

### 3.3 特征质量审计与泄漏防控

处理后的训练集保留原始78维数值特征，不依据测试集结果删除常量或高相关变量。训练集审计发现12个常量特征、18个近零方差特征；训练、验证和测试特征矩阵之间的精确重复数均为0。特征质量表记录每列数据类型、最小值、最大值、均值、标准差和唯一值比例，仅用于描述和敏感性分析。

泄漏防控分为三层。样本层：全局去重和跨标签冲突处理后再分层划分，并对三个split执行精确特征向量重叠检查。变换层：Min-Max归一化器只在训练集拟合，验证集和测试集只调用`transform`；χ²、互信息、ANOVA和特征排序只由训练集计算。模型层：超参数选择在训练集内交叉验证完成，验证集只用于加权随机森林的单树评分，测试集仅用于最终报告。来源字段、文件名、时间戳和原始标签均不进入模型输入。

UNSW-NB15采用官方训练/测试文件，不与CIC-IDS2017混合建模。类别特征的独热编码词表只由训练集确定，测试集列按训练集列对齐；归一化器、χ²排序和标签编码同样只在训练集拟合。两套数据的处理协议不同，正文分别报告，不直接比较绝对指标。

'''

def main():
    text = SOURCE.read_text(encoding="utf-8")
    start = text.index("### 3.2 数据审计与样本构建")
    end = text.index("### 3.3 无泄漏预处理和参数选择")
    text = text[:start] + SECTION + text[end:].replace("### 3.3 无泄漏预处理和参数选择", "### 3.4 无泄漏预处理和参数选择", 1)
    text = text.replace("### 3.4 加权随机森林", "### 3.5 加权随机森林")
    text = text.replace("### 3.5 复杂度和实现边界", "### 3.6 复杂度和实现边界")
    external = '''### 4.5 文件外泛化与类别覆盖感知评估

为补充流级随机分层划分，本文实施留一文件外评估：每轮将一个原始CIC-IDS2017文件完全作为测试文件，其余文件作为训练来源，并对训练侧每类最多抽取5,000条样本。由于原始文件的攻击类别覆盖并不完整，评估首先报告训练集已知标签与测试文件未见标签；Accuracy、Balanced Accuracy和Macro-F1只在已知标签样本上计算，未见标签比例单独作为覆盖风险指标。结果显示，WebAttacks文件中的Web Attack属于训练未见类别，Friday Morning文件中的Bot也属于训练未见类别；在已知类别上的Macro-F1从0.3319（Wednesday文件）到0.9997（Friday Morning文件）不等。这一范围表明文件间分布差异远大于固定流级划分下的随机波动。文件外结果用于揭示时间/场景外泛化风险，不构成完整五分类泛化成功证明，也不应与平衡研究子集的测试指标直接比较。

'''
    marker = "### 4.5 Bootstrap置信区间"
    if marker in text:
        text = text.replace(marker, external + marker.replace("### 4.5", "### 4.6"), 1)
    for old, new in [("### 4.6 特征选择公平对照与概率质量", "### 4.7 特征选择公平对照与概率质量"), ("### 4.7 离线推理性能与扰动敏感性", "### 4.8 离线推理性能与扰动敏感性"), ("### 4.8 不平衡敏感性", "### 4.9 不平衡敏感性"), ("### 4.9 UNSW-NB15独立公开数据集基准", "### 4.10 UNSW-NB15独立公开数据集基准"), ("### 4.10 UNSW-NB15类别级指标和少数类分析", "### 4.11 UNSW-NB15类别级指标和少数类分析"), ("### 4.11 UNSW-NB15结果边界", "### 4.12 UNSW-NB15结果边界"), ("### 4.12 NSL-KDD独立公开数据集基准", "### 4.13 NSL-KDD独立公开数据集基准"), ("### 4.13 NSL-KDD少数类补充分析", "### 4.14 NSL-KDD少数类补充分析"), ("### 4.14 文件级泛化可行性与研究限制", "### 4.15 文件级泛化可行性与研究限制"), ("### 4.15 结论边界", "### 4.16 结论边界")]:
        text = text.replace(old, new)
    OUT.write_text(text, encoding="utf-8")
    print(f"WROTE={OUT}")

if __name__ == "__main__":
    main()
