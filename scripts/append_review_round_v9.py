"""Record the ninth round: the full read-through."""
from __future__ import annotations

import sys
from pathlib import Path

sys.stdout.reconfigure(encoding="utf-8", errors="replace")
ROOT = Path(__file__).resolve().parents[1]
MD = ROOT / "重构版论文_v4_20260915" / "遗漏问题审查报告.md"

ADDITION = """

---

## O. 第九轮审查（2026-09-17）：全篇通读复核

前八轮全部是**逐项检查**（脚本比对、跨文档比对）。这一轮改为**从头到尾连读**，目标是找出脚本查不出、只有人读才发现的缺陷。共发现 14 项，其中 4 项属于数据自相矛盾。

### 最严重：正文被参考文献标注污染

§3.4 协议第 5 条、§4.5 对照第 5 条、§6.4 建议第 5 条以及算法 1 的第 5 步，**中英各 4 处**被追加了 `[no DOI; NeurIPS proceedings]`。原因是修参考文献的脚本用"数字 + 句点"匹配条目，误伤了同样以"5. "开头的编号段落。中文还多出一个句点与一处重复 ISBN。脚本检查只覆盖了参考文献区，因此前八轮全部漏检。

### 数据自相矛盾（4 项）

| 位置 | 问题 |
|---|---|
| §5.2 第四条 | 同一段先写"整批推理慢约 5.3 倍"，紧接着说"5.9 是整批推理之比"——旧数字未随十种子更新 |
| §5.2 第四条 | 训练耗时写 61.0 s 对 0.78 s，而表 4(a) 已改为 93.3 s 对 1.16 s |
| §6.2 第 2 条 | 类别先验效应写 +0.0719，与 5.4 节和结论的 +0.0725 不一致 |
| §6.2 第 1 条与 §5.4 | 声称协议效应"比任何模型间差异高一个数量级（最大 0.0078）"，但模型族差距为 0.0318 与 0.0916，**比该协议效应更大**——这是与已收窄标题同源的残留过度声称 |

### 其他问题（6 项）

| 位置 | 问题 | 处置 |
|---|---|---|
| 表 4(a) | 表头**重复出现两次**（表格替换时旧表头未删） | 已删 |
| §1.4 贡献 3 | 写"四类差异来源"，实际列举五类 | 改为五类 |
| §4.3 条件 3 | 残留"此前的纯定义式表述使用归一化权重熵"——读者不知道存在"此前版本" | 改为"对应的可观测判据是…" |
| 表 5 评注 | 称极端随机树差距是"唯一实质性的模型间差距"，但 MLP 差距 0.092 更大 | 已改为并列说明两处差距 |
| 全文 | **12 处引文标注落在句号之后**（应为 `... detection [18-20].` 而非 `... detection. [18-20]`） | 全部前移 |
| §1.2 / §2.5 / §7 | 若干"协议效应与模型效应"措辞未随标题收窄同步 | 统一为"聚合规则差异" |

### 新增防护

新增 `scripts/body_hygiene_check_v9.py` 并接入验证闸门，覆盖本轮发现的全部缺陷类别：正文中的参考文献标注、句号后的引文、重复表头、重复标点、悬空的编辑残留、残留的过度声称、重复的 ISBN 字段。闸门现含 **9 类检查**，全部通过。

### 观察

本轮 14 项中有 10 项是**我自己前几轮编辑留下的痕迹**，而不是最初写作的问题。说明多轮自动编辑会持续引入新的不一致，仅靠增量修补不够——这也正是把卫生检查固化进闸门的理由。
"""


def main() -> None:
    text = MD.read_text(encoding="utf-8")
    if "## O. 第九轮审查" in text:
        print("already recorded")
        return
    MD.write_text(text.rstrip() + ADDITION, encoding="utf-8")
    print("ROUND9_RECORDED")


if __name__ == "__main__":
    main()
